    
pipeline {
    agent any
    stages {
        stage("build") {
            steps {
                cleanWs()
                checkout scm
                sh '''
                    cd Maven-project/boxfuse-sample-java-war-hello-master/
                    mvn package
                '''
            }
        }
    }
 }
