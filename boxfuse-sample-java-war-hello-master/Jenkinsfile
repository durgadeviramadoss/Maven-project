    
pipeline {
    agent any
    stages {
        stage("build") {
            steps {
                cleanWs()
                checkout scm
                sh '''
                    ls -l
                   # cd Maven-project/boxfuse-sample-java-war-hello-master/
                   # mvn package
                '''
            }
        }
    }
 }
